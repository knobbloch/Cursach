<Application x:Class="ApplicationProject.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:ApplicationProject"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ControlTemplate x:Key="ButtonEllipseTextOverlay" TargetType="Button">
            <Grid>
                <Ellipse Fill="{TemplateBinding Background}" />
                <ContentPresenter Content="{TemplateBinding Content}" HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Grid>
        </ControlTemplate>
        <ControlTemplate x:Key="ButtonEllipseTextBelow" TargetType="Button">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Ellipse Fill="{TemplateBinding Background}" Width="{Binding ActualHeight, RelativeSource={RelativeSource Self}}" />
                <ContentPresenter Content="{TemplateBinding Content}" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Row="1" />
            </Grid>
        </ControlTemplate>
    </Application.Resources>
</Application>
