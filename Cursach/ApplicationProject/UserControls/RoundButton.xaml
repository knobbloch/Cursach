<UserControl x:Class="ApplicationProject.UserControls.RoundButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ApplicationProject.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="450" DataContext="{Binding RelativeSource={RelativeSource Self}}">
    
    <Button Background="Transparent" Click="OnClick" BorderThickness="0" Padding="0">
        <Button.Content>
            <Grid Width="{Binding ActualWidth}" Height="{Binding ActualHeight}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Ellipse Fill="{Binding ButtonColor}" Width="{Binding ActualHeight, RelativeSource={RelativeSource Self}}" />
                <ContentControl Content="{Binding ButtonContent}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                <ContentControl Content="{Binding UnderContent}" VerticalAlignment="Center" HorizontalAlignment="Center" Grid.Row="1" />
            </Grid>
        </Button.Content>
    </Button>
</UserControl>